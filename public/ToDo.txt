## Now

## Next
- Make User B know if User A is focussed on a field.
- Handle Fixed Secondaries...
- MVP Hall of fame.


## Horizon
- Auth
    - Avatars (general or army?)
    - Highlight animation a field that was updated: https://www.pluralsight.com/guides/prop-changes-in-react-component
- Game dashboard stuff!
- Work out how to use Crests
    - Not every army will have one

- Detachment Options list when army selected.
- Ability to set up events (e.g. tournies)

- Bring unit data in for armies we own the codex of

- Think about army/general relationship - an army could be played by more than 1 general

- Compact Match summary for completed matches at a glance

- light mode?

- Domain Name

- Tech debt:
    - Firestore listener consolidation Currently getCollectionSnapshot is called independently on multiple pages/components, creating separate real-time listeners per call. getDocSnapshot is also called per-battle inside RecentForm (once for general alias, once for army name) creating additional listeners at render time. Consider moving Armies, Battles, and Generals collections into shared React Contexts (similar to EditionContext) so each collection is fetched once and shared app-wide — RecentForm could then look up names from context rather than opening new listeners. Low priority at current scale but worth revisiting if user count or Firestore usage grows.

